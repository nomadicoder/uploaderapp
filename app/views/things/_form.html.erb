<%= form_for(thing) do |f| %>
  <% if thing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(thing.errors.count, "error") %> prohibited this thing from being saved:</h2>

      <ul>
      <% thing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :datafile %>
    <%= f.file_field :fileupload, multiple: false, name: "thing[datafile]" %>
    <div id="progress">
        <div class="bar" style="width: 0%;"></div>
    </div>
  </div>

<% end %>

